using System.Collections;
using System.Collections.Generic;
using UnityEngine; 

public class nullArea : MonoBehaviour
{
   public ProximityActivate[] nullActivator;
    public Collision nullAreaTransform;

    /* private void OnCollisionExit(Collider other)
     {
         if (other.gameObject.CompareTag("Sharps") || gameObject.CompareTag("BioHazard") || gameObject.CompareTag("Trash"))
         {
             nullActivator.activeState = true;
         }
     } */
}
